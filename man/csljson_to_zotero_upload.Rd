% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csljson_to_zotero_upload.R
\name{csljson_to_zotero_upload}
\alias{csljson_to_zotero_upload}
\title{Upload CSL JSON batch files to a Zotero group library (with progress bar)}
\usage{
csljson_to_zotero_upload(
  files,
  group_id,
  api_key = Sys.getenv("ZOTERO_API_KEY"),
  pause = 0.5
)
}
\arguments{
\item{files}{Character vector. Paths to CSL-JSON files. If a single element
points to an existing directory, all \verb{*.json} files in that directory
(non-recursive) are used.}

\item{group_id}{Character or numeric. Zotero \strong{group} ID.}

\item{api_key}{Character. Zotero API key with write access to the group.
Defaults to \code{Sys.getenv("ZOTERO_API_KEY")}.}

\item{pause}{Numeric scalar. Number of seconds to wait between requests
(to avoid rate limiting). Default is \code{0.5}.}
}
\value{
A data.frame with one row per file and columns:
\describe{
\item{file}{Path to the CSL-JSON file.}
\item{status_code}{HTTP status code returned by the Zotero API.}
\item{ok}{Logical; \code{TRUE} if \code{status_code} is in 200–299.}
\item{message}{Character; short message or error text (possibly truncated).}
}
Invisibly returns this data.frame.
}
\description{
Upload one or more CSL-JSON files to a Zotero \strong{group} library using the
Zotero Web API. Each file is assumed to contain an array of item objects
suitable for POSTing to \verb{/groups/\{group_id\}/items}.

Progress is reported via the \pkg{progressr} package, which integrates with
\pkg{future} / \pkg{future.apply} progress handlers.
}
\examples{
\dontrun{
library(progressr)
handlers(global = TRUE)
handlers("cli")  # or "progress", "txtprogressbar", etc.

res <- csljson_to_zotero_upload(
  files    = "csljson_batches",  # directory with items_0001.json, ...
  group_id = "123456",
  api_key  = Sys.getenv("ZOTERO_API_KEY")
)

subset(res, !ok)  # inspect failures
}

}
